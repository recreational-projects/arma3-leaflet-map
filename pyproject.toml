[project]
name = "arma3-leaflet-map"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "folium>=0.19.5",
    "msgspec>=0.19.0",
    "rich>=14.3.3",
]

[dependency-groups]
dev = [
    "ruff>=0.15.2",
    "ty>=0.0.18",
]
